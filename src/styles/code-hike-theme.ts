const theme = {
  lineNumbers: false,
  showCopyButton: true,
  theme: 'poimandres',
  skipLanguages: ['mermaid'],
  staticMediaQuery: 'not screen, (max-width: 768px)',
  autoImport: false,
}

export default theme
